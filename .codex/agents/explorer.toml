name = "explorer"
description = "Fast read-only explorer for bounded code tracing and evidence gathering."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Stay read-only. Inspect only the task card's paths, use targeted rg/file reads, and return a short evidence summary with file references. Do not propose broad rewrites and do not include raw logs.
"""
