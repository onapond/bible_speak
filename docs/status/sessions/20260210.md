# 세션 요약 - 2026년 02월 10일

## 완료된 작업

### 1. 개발 품질 근본 개선 — 시니어 개발자 마인드셋 정착

#### Part 1: 프로세스 규칙 강화
- `CLAUDE.md`에 "시니어 개발자 원칙" 섹션 추가 (구현 전 영향분석 / 구현 중 사용자 관점 사고 / 구현 후 회귀 점검)
- `CLAUDE.md`에 "변경 유형별 필수 점검표" 추가 (색상·폰트·서비스·위젯 변경 시 체크리스트)
- `docs/dev/DEVELOPMENT_RULES.md` §4.7 하드코딩 색상 금지 규칙, §4.8 커밋 전 UI 자동 검사 규칙 추가

#### Part 2: 자동 감지 도구 구축
- `scripts/ui_check.dart` 신규 작성 — 정적 분석 스크립트
  - Colors.white 텍스트 색상 사용 감지 (밝은 배경 파일에서 → error, 기타 → warning)
  - Colors.black 직접 사용 감지 (→ warning)
  - Color(0xFFFFFFFF) / Color(0xFF000000) 하드코딩 감지
  - `// ui-check-ignore` 인라인 무시 지원
  - `--summary` 옵션으로 요약만 출력 가능
- `.claude/hooks/pre-commit-check.sh` 보강 — flutter analyze + ui_check.dart 2단계 검사 (error → 커밋 차단, warning → 표시만)

#### Part 3: 감지된 문제 일괄 수정
- 6개 error 수정: login_screen, my_page_screen, goal_setup_screen에서 `Colors.white` → `ParchmentTheme.softPapyrus`
- **한글 폰트 근본 원인 수정**: `main.dart`에서 기본 `ThemeData(primarySwatch: Colors.indigo)` 대신 `ParchmentTheme.lightTheme` 적용
- `GoogleFonts.notoSansKrTextTheme(ParchmentTheme.lightTheme.textTheme)`으로 폰트+색상 병합

### 2. 암송 학습 화면 레이아웃 개선
- 진척도바(`_buildProgressBar`)를 고정 영역에서 스크롤 영역으로 이동
- 단계 인디케이터 + 구절 네비게이터만 상단 고정 유지 → 암송 콘텐츠 영역 확대

### 3. 그룹 선택 화면 레이아웃 개선
- `SizedBox(height: 280)` + `TabBarView` → 탭 인덱스 기반 조건 렌더링으로 교체
- 그룹 목록 `Expanded + ListView.builder` → `Column` spread 리스트로 변경
- 부모 스크롤에 자연스럽게 통합, 화면 크기 자동 적응

## 수정된 파일

| 파일 | 변경 내용 |
|------|----------|
| `CLAUDE.md` | 시니어 개발자 원칙 + 변경 유형별 점검표 추가 |
| `docs/dev/DEVELOPMENT_RULES.md` | §4.7 하드코딩 색상 금지, §4.8 커밋 전 UI 검사 |
| `scripts/ui_check.dart` | **신규** — UI 품질 자동 감지 스크립트 |
| `.claude/hooks/pre-commit-check.sh` | ui_check.dart 실행 단계 추가 |
| `lib/main.dart` | ParchmentTheme.lightTheme 적용 + Noto Sans KR 병합 |
| `lib/screens/auth/login_screen.dart` | Colors.white → ParchmentTheme.softPapyrus (2건) |
| `lib/screens/mypage/my_page_screen.dart` | Colors.white → ParchmentTheme.softPapyrus (2건) |
| `lib/screens/onboarding/goal_setup_screen.dart` | Colors.white → ParchmentTheme.softPapyrus (2건) |
| `lib/screens/practice/verse_practice_screen.dart` | 진척도바를 스크롤 영역으로 이동 |
| `lib/screens/group/group_select_screen.dart` | 고정 높이 TabBarView → 동적 탭 렌더링 |

## 커밋 이력

| 커밋 | 메시지 |
|------|--------|
| `efddd50` | feat: Add UI quality check system & apply ParchmentTheme globally |
| `9d8ba74` | fix: Move progress bar into scroll area on verse practice screen |
| `1f3d7f5` | fix: Replace fixed-height TabBarView with dynamic tab rendering in group select |

## 현재 상태
- UI 체크 스크립트: **0 errors**, 276 warnings (Colors.white/black 하드코딩 — 점진적 수정 대상)
- flutter analyze: 통과 (info/warning만)
- 배포: https://bible-speak.web.app 정상 동작 확인

## 다음 작업 (TODO)
- 276개 warnings (Colors.white/black 하드코딩)를 점진적으로 ParchmentTheme 상수로 교체
- 특히 `verse_practice_screen.dart` (60건), `group_select_screen.dart` (30건) 등 집중 파일 우선 처리
- 각 화면별 어두운 배경(`_bgColor`, `_cardColor`) 위 Colors.white 사용은 해당 테마 상수로 통일

## 중요 참고사항
- `main.dart`의 테마가 `ParchmentTheme.lightTheme`으로 변경됨 — 이전에 `Theme.of(context)`로 기본 Material 색상에 의존하던 코드가 있다면 확인 필요
- `scripts/ui_check.dart`는 `dart run scripts/ui_check.dart`로 수동 실행 가능, 커밋 시 자동 실행됨
- 인라인 무시가 필요하면 줄 끝에 `// ui-check-ignore` 추가
