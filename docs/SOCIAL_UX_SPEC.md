# Bible Speak 소셜 UX 상세 기획서

**Version**: 2.0
**Last Updated**: 2026-01-27
**Author**: CTO Planning

---

## Executive Summary

Bible Speak의 핵심 가치는 **"혼자가 아닌 함께 암송"**입니다.
본 기획서는 사용자들이 매일 앱을 열고, 그룹원들과 서로 독려하며 성경 암송을 지속할 수 있는 소셜 UX 시스템을 정의합니다.

### 핵심 원칙

| 원칙 | 설명 |
|------|------|
| **One Tap = Max Impact** | 한 번의 탭으로 최대의 사회적 효과 |
| **Cost-Efficient Sync** | Firestore 읽기 비용 최소화 |
| **Warm Christianity** | 경쟁보다는 격려, 순위보다는 동행 |
| **Habit Formation** | 21일 습관 형성 이론 적용 |

### 사용자 페르소나

| 페르소나 | 특징 | 니즈 | 핵심 기능 |
|----------|------|------|----------|
| **열정 신자** | 매일 성경 읽기, 30대 직장인 | 체계적인 암송 시스템, 진도 관리 | 스트릭, 달란트 |
| **새신자** | 성경 처음 접함, 20대 | 쉬운 시작점, 격려와 동기부여 | AI 코칭, 그룹 피드 |
| **그룹 리더** | 셀 모임 인도자, 40대 | 멤버 관리, 참여율 모니터링 | 대시보드, 넛지 |
| **간헐적 사용자** | 가끔 접속, 바쁜 직장인 | 부담 없는 참여, 리마인더 | 아침 만나, 알림 |

---

## 1. Daily Engagement System (일일 참여 시스템)

### 1.1 Daily Streak (연속 학습 스트릭)

**목적**: 매일 앱을 여는 습관 형성 (21일 습관 형성 이론 기반)

#### UI 디자인

```
┌─────────────────────────────────────────────┐
│  🔥 7일 연속 학습 중!                        │
│  ████████████░░░░░░░░  14/21일 목표          │
│                                              │
│  월  화  수  목  금  토  일                  │
│  🔥  🔥  🔥  🔥  🔥  🔥  ⭕                  │
│                        오늘                  │
└─────────────────────────────────────────────┘
```

#### 1.1.1 스트릭 계산 규칙

| 조건 | 스트릭 변화 | 상세 설명 |
|------|-------------|----------|
| 오늘 1절 이상 Stage 1 통과 | 유지/+1 | 해당 날짜 00:00-23:59 기준 |
| 자정까지 미완료 | 리셋 (0) | 다음날 00:00 서버 시간 기준 |
| 스트릭 보호권 사용 | 유지 | 달란트 100개 소모, 월 2회 제한 |
| 2일 이상 연속 미접속 | 리셋 (0) | 보호권으로도 커버 불가 |

#### 1.1.2 스트릭 마일스톤 보상

| 연속일 | 달란트 보상 | 배지 | 칭호 | 특별 효과 |
|--------|------------|------|------|----------|
| 3일 | 5개 | 🔥 | - | 첫 불꽃 애니메이션 |
| 7일 | 15개 | 🏅 | "일주일 전사" | 프로필 불꽃 표시 |
| 14일 | 30개 | 🥈 | "2주 챔피언" | 그룹 내 알림 발송 |
| 21일 | 50개 | 🥇 | "습관 마스터" | 특별 프로필 아이콘 |
| 30일 | 100개 | 💎 | "한 달의 기적" | 그룹 리더보드 상단 표시 |
| 100일 | 500개 | 👑 | "백일 성자" | 금색 프로필 테두리 |
| 365일 | 2000개 | 🌟 | "1년 헌신자" | 특별 이모지 + 전체 공지 |

#### 1.1.3 스트릭 보호 시스템

**보호권 구매 UI**:
```
┌─────────────────────────────────────────────┐
│  ⚠️ 스트릭이 끊어질 위험!                    │
│                                              │
│  현재 스트릭: 🔥 14일                        │
│  마지막 학습: 어제 오후 3:24                  │
│                                              │
│  스트릭 보호권을 사용하시겠습니까?            │
│  (달란트 100개 소모)                         │
│                                              │
│  보유 달란트: 💰 250개                       │
│  이번 달 사용: 1/2회                         │
│                                              │
│  [보호권 사용]         [괜찮아요]             │
└─────────────────────────────────────────────┘
```

**보호권 규칙**:
| 규칙 | 내용 |
|------|------|
| 최소 스트릭 | 7일 이상일 때만 구매 가능 |
| 월간 제한 | 2회/월 (매월 1일 리셋) |
| 가격 | 달란트 100개 (고정) |
| 표시 | 캘린더에 🛡️ 아이콘으로 표시 |
| 연속 사용 | 2일 연속 사용 불가 |

#### 1.1.4 스트릭 끊김 시 복구 유도

```
┌─────────────────────────────────────────────┐
│  😢 아쉽게도 스트릭이 끊어졌어요              │
│                                              │
│  이전 기록: 🔥 14일 (최고 기록!)              │
│  현재: 0일                                   │
│                                              │
│  💪 다시 시작해볼까요?                       │
│  오늘 시작하면 내일 2일째!                   │
│                                              │
│  [오늘의 구절 시작하기]                      │
└─────────────────────────────────────────────┘
```

#### 1.1.5 Firestore Schema

```javascript
// Collection: users/{uid}
// Document field: streak (embedded)
{
  streak: {
    currentStreak: 7,              // 현재 연속일
    longestStreak: 14,             // 최장 기록
    lastLearnedDate: "2026-01-27", // 마지막 학습일 (YYYY-MM-DD)
    streakStartDate: "2026-01-21", // 현재 스트릭 시작일
    protectionUsedThisMonth: 1,    // 이번 달 보호권 사용 횟수
    protectedDates: ["2026-01-15"], // 보호권 사용 날짜 목록
    weeklyHistory: [true, true, true, true, true, true, false], // 최근 7일
    milestones: {                  // 달성한 마일스톤
      "7": "2026-01-20",
      "14": "2026-01-27"
    },
    totalDaysLearned: 45           // 총 학습일 (비연속 포함)
  }
}
```

**비용 최적화**:
- `lastLearnedDate`만 매일 업데이트 (1 write/day)
- 스트릭 계산은 클라이언트에서 수행
- `weeklyHistory`는 앱 시작 시 계산하여 표시
- 마일스톤은 달성 시점에만 write

---

### 1.2 Morning Manna (아침 만나)

**목적**: 오전 6-8시 골든타임에 앱 진입 유도

#### UI 디자인

```
┌─────────────────────────────────────────────┐
│  ☀️ 오늘의 만나                      1월 27일│
│                                              │
│  "여호와를 경외하는 것이 지혜의 근본이라"      │
│   - 잠언 9:10                                │
│                                              │
│  ⏰ Early Bird 보너스: +2 달란트 (06:32)      │
│                                              │
│  [오늘의 구절 암송하기]                       │
└─────────────────────────────────────────────┘
```

#### 1.2.1 오늘의 구절 선정 알고리즘

**선정 우선순위**:
1. **개인 맞춤** - 사용자가 학습 중인 책의 다음 구절
2. **그룹 트렌드** - 그룹에서 가장 많이 학습 중인 구절
3. **시즌 구절** - 특별 절기에 맞는 구절
4. **명구절 풀** - 100개 큐레이션된 명구절 중 랜덤

**시즌별 구절 풀**:

| 시즌 | 기간 | 추천 구절 |
|------|------|----------|
| 신년 | 1/1-1/7 | 여호수아 1:9, 잠언 16:3, 예레미야 29:11 |
| 사순절 | 부활절 40일 전 | 마태복음 수난기사, 이사야 53장 |
| 부활절 | 부활절 주간 | 고전 15:55-57, 요한복음 11:25-26 |
| 어버이날 | 5/8 전후 | 출애굽기 20:12, 잠언 31:28 |
| 추수감사절 | 11월 넷째 주 | 시편 100편, 데살로니가전서 5:18 |
| 성탄절 | 12/20-12/25 | 누가복음 2:10-14, 이사야 9:6 |

#### 1.2.2 Early Bird 보너스 시스템

| 시간대 | 보너스 | 메시지 |
|--------|--------|--------|
| 05:00-06:00 | 달란트 +3 | "🌅 새벽 기도의 시간!" |
| 06:00-07:00 | 달란트 +2 | "☀️ 아침 골든타임!" |
| 07:00-08:00 | 달란트 +1 | "🌤️ 좋은 아침이에요!" |
| 08:00 이후 | 기본 (0) | "오늘도 화이팅!" |

**보너스 획득 조건**:
- 해당 시간대에 **Stage 1 통과** 시 지급
- 하루 1회만 지급 (가장 이른 시간 적용)
- 앱 실행만으로는 미지급

#### 1.2.3 알림 설정 옵션

```
┌─────────────────────────────────────────────┐
│  🔔 아침 만나 알림 설정                       │
│                                              │
│  알림 시간:                                  │
│  ○ 새벽 5시 (🌅 +3 달란트)                   │
│  ● 아침 6시 (☀️ +2 달란트) ← 선택됨          │
│  ○ 아침 7시 (🌤️ +1 달란트)                   │
│  ○ 아침 8시                                  │
│  ○ 알림 끄기                                 │
│                                              │
│  [저장]                                      │
└─────────────────────────────────────────────┘
```

#### 1.2.4 Cloud Function 구현

```javascript
// 매일 자정 실행 (KST 00:00)
exports.selectDailyVerse = functions.pubsub
  .schedule('0 0 * * *')
  .timeZone('Asia/Seoul')
  .onRun(async (context) => {
    const today = new Date();
    const seasonVerse = getSeasonVerse(today);
    const defaultVerses = await getDefaultVersePool();

    const dailyVerse = seasonVerse || randomSelect(defaultVerses);

    await admin.firestore()
      .collection('global')
      .doc('dailyVerse')
      .set({
        reference: dailyVerse.reference,
        text: dailyVerse.text,
        textKo: dailyVerse.textKo,
        date: today.toISOString().split('T')[0],
        season: seasonVerse ? 'seasonal' : 'default'
      });
  });
```

---

### 1.3 달란트 경제 시스템

#### 1.3.1 달란트 획득 방법

| 활동 | 획득량 | 일일 한도 | 조건 |
|------|--------|----------|------|
| Stage 1 통과 | 1개 | 무제한 | 70% 이상 |
| Stage 2 통과 | 2개 | 무제한 | 75% 이상 |
| Stage 3 통과 | 3개 | 무제한 | 80% 이상 |
| Early Bird 보너스 | 1-3개 | 1회/일 | 시간대별 |
| 스트릭 3일 달성 | 5개 | 해당 시 | 최초 1회 |
| 스트릭 7일 달성 | 15개 | 해당 시 | 최초 1회 |
| 스트릭 21일 달성 | 50개 | 해당 시 | 최초 1회 |
| 그룹 주간 챌린지 완료 | 5개 | 주 1회 | 그룹 목표 달성 시 |
| 친구 초대 성공 | 50개 | 무제한 | 초대받은 친구가 3일 연속 학습 |
| 첫 그룹 가입 | 20개 | 1회 | 최초 그룹 가입 시 |
| 첫 Stage 3 통과 | 10개 | 1회 | 어떤 구절이든 첫 통과 |
| 책 완독 (예: 말라기) | 30개 | 책당 1회 | 해당 책 모든 절 Stage 3 |

#### 1.3.2 달란트 사용처

| 아이템 | 가격 | 설명 | 유효기간 |
|--------|------|------|----------|
| 스트릭 보호권 | 100개 | 하루 스트릭 유지 | 즉시 사용 |
| 프로필 테두리 (골드) | 200개 | 금색 프로필 테두리 | 영구 |
| 프로필 테두리 (플래티넘) | 500개 | 반짝이 테두리 | 영구 |
| 특별 이모지 팩 | 150개 | 반응용 이모지 6종 | 영구 |
| 그룹 부스터 | 300개 | 그룹 목표 기여 2배 (1일) | 24시간 |
| 광고 제거 (7일) | 500개 | 배너 광고 미노출 | 7일 |
| 광고 제거 (30일) | 1500개 | 배너 광고 미노출 | 30일 |

#### 1.3.3 Firestore Schema

```javascript
// Collection: users/{uid}
// Document field: dalants (embedded)
{
  dalants: {
    balance: 250,                  // 현재 보유량
    totalEarned: 1500,             // 총 획득량
    totalSpent: 1250,              // 총 사용량
    lastEarnedAt: Timestamp,       // 마지막 획득 시간
    history: [                     // 최근 50개 기록만 유지
      {
        type: "earn",
        amount: 3,
        reason: "stage3_pass",
        verseRef: "말라기 1:5",
        createdAt: Timestamp
      }
    ]
  }
}
```

---

## 2. Group Activity Feed (그룹 활동 피드)

### 2.1 Live Activity Ticker

**목적**: 그룹원들의 실시간 활동을 보여주어 "나도 해야지" 동기 부여

#### UI 디자인 (홈 화면)

```
┌─────────────────────────────────────────────┐
│  👥 은혜의 샘 그룹 소식              [더보기]│
│  ─────────────────────────────────────────  │
│  🎉 김성실 님이 말라기 1:5 암송 완료! (2분 전)│
│     └ 👏 3  🙏 2  💪 1     [응원하기]        │
│  🔥 박믿음 님이 7일 연속 학습 달성! (15분 전) │
│     └ 👏 8  🙏 5  💪 3     [축하하기]        │
│  🙏 이소망 님이 에베소서 시작! (1시간 전)    │
│     └ 👏 2  🙏 4  💪 1     [응원하기]        │
│                                              │
│  ─────────────────────────────────────────  │
│  📊 오늘 그룹 활동: 12명 참여, 34절 암송     │
└─────────────────────────────────────────────┘
```

#### 2.1.1 Activity Types & Templates

| Type | 아이콘 | 메시지 템플릿 | 트리거 조건 |
|------|--------|--------------|-------------|
| `verse_complete` | 🎉 | `{name}님이 {verse} 암송 완료!` | Stage 1 통과 |
| `stage3_clear` | ⭐ | `{name}님이 {verse} 완전 암기!` | Stage 3 통과 |
| `streak_milestone` | 🔥 | `{name}님이 {n}일 연속 학습!` | 3/7/14/21/30일 |
| `book_complete` | 📖 | `{name}님이 {book} 완독!` | 책 전체 Stage 3 |
| `joined_group` | 👋 | `{name}님이 그룹에 합류!` | 그룹 가입 |
| `book_start` | 🙏 | `{name}님이 {book} 시작!` | 새 책 첫 절 시작 |
| `comeback` | 🎊 | `{name}님이 돌아왔어요!` | 7일 이상 미접속 후 복귀 |

#### 2.1.2 Activity 생성 규칙

**중복 방지**:
- 같은 구절 `verse_complete`는 하루 1회만 생성
- `streak_milestone`은 해당 마일스톤당 1회만
- 5분 내 동일 유형 활동 병합 (예: "3절 연속 암송!")

**Privacy 설정**:
```
┌─────────────────────────────────────────────┐
│  🔒 활동 공개 설정                           │
│                                              │
│  ● 모든 활동 공개                            │
│  ○ 마일스톤만 공개 (7일+, 책 완독 등)        │
│  ○ 그룹에 공개 안함                          │
│                                              │
│  [저장]                                      │
└─────────────────────────────────────────────┘
```

#### 2.1.3 Firestore Schema

```javascript
// Collection: groups/{groupId}/activities/{activityId}
{
  id: "act_abc123",
  type: "verse_complete",          // Activity type
  userId: "user123",
  userName: "김성실",
  userPhotoUrl: "https://...",

  // Type-specific fields
  verseRef: "말라기 1:5",          // for verse_complete, stage3_clear
  milestone: 7,                    // for streak_milestone
  bookName: "말라기",              // for book_complete, book_start

  // Engagement
  reactions: {
    clap: ["user456", "user789"],  // 👏 박수
    pray: ["user456"],             // 🙏 기도
    fighting: ["user789"]          // 💪 화이팅
  },
  reactionCounts: {
    clap: 2,
    pray: 1,
    fighting: 1
  },

  // Metadata
  createdAt: Timestamp,
  expiresAt: Timestamp,            // 7일 후 자동 삭제
  isHidden: false                  // 사용자가 숨김 처리
}
```

#### 2.1.4 비용 최적화 전략

| 전략 | 구현 방법 | 절감 효과 |
|------|----------|----------|
| **TTL 자동 삭제** | `expiresAt` 필드 + Cloud Function | 저장 비용 -70% |
| **Pagination** | 최근 20개만 로드, 스크롤 시 추가 | 읽기 비용 -50% |
| **Local Cache** | `lastFetchTime` 이후만 쿼리 | 읽기 비용 -80% |
| **Batch Write** | 5분마다 활동 일괄 생성 | 쓰기 비용 -40% |
| **Reaction Count 캐시** | 배열 대신 count 필드 사용 | 읽기 비용 -20% |

**쿼리 최적화 코드**:
```dart
// 마지막 fetch 이후 새 활동만 가져오기
Query<Map<String, dynamic>> getNewActivities(
  String groupId,
  DateTime lastFetchTime,
) {
  return _firestore
    .collection('groups/$groupId/activities')
    .where('createdAt', isGreaterThan: Timestamp.fromDate(lastFetchTime))
    .where('isHidden', isEqualTo: false)
    .orderBy('createdAt', descending: true)
    .limit(20);
}
```

---

### 2.2 Group Dashboard (그룹 대시보드)

**목적**: 그룹 전체 현황 한눈에 파악

#### UI 디자인

```
┌─────────────────────────────────────────────┐
│  📊 은혜의 샘 그룹 대시보드                   │
│                                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐        │
│  │ 👥 12명  │ │ 🔥 8명  │ │ 📖 156절│        │
│  │ 전체 멤버│ │오늘 참여│ │이번 주  │        │
│  └─────────┘ └─────────┘ └─────────┘        │
│                                              │
│  ─────────────────────────────────────────  │
│  📈 이번 주 진도                             │
│  ████████████████░░░░  80% (목표 200절)     │
│                                              │
│  ─────────────────────────────────────────  │
│  🏆 이번 주 MVP                              │
│  1. 김성실 🔥14일 - 25절                     │
│  2. 박믿음 🔥7일 - 18절                      │
│  3. 이소망 🔥5일 - 15절                      │
│                                              │
│  ─────────────────────────────────────────  │
│  😴 독려가 필요해요                          │
│  최요셉 (5일 미접속)  [💌 찌르기]            │
│  한은혜 (3일 미접속)  [💌 찌르기]            │
└─────────────────────────────────────────────┘
```

#### 2.2.1 대시보드 지표

| 지표 | 계산 방법 | 갱신 주기 |
|------|----------|----------|
| 전체 멤버 수 | `members` 컬렉션 count | 실시간 |
| 오늘 참여자 수 | `lastLearnedDate == today` 카운트 | 5분 |
| 이번 주 암송 절 수 | 주간 activities 합계 | 5분 |
| 목표 달성률 | (현재/목표) × 100 | 5분 |
| MVP 랭킹 | 주간 암송 절 수 기준 정렬 | 1시간 |

#### 2.2.2 Firestore Schema (Group Stats)

```javascript
// Collection: groups/{groupId}
// Document: stats (subcollection 또는 embedded)
{
  stats: {
    memberCount: 12,
    todayActiveCount: 8,           // 매일 자정 리셋
    weeklyVerseCount: 156,         // 매주 월요일 리셋
    weeklyGoal: 200,
    lastUpdated: Timestamp,

    // 주간 MVP (Cloud Function으로 1시간마다 계산)
    weeklyMvp: [
      { uid: "user1", name: "김성실", streak: 14, verses: 25 },
      { uid: "user2", name: "박믿음", streak: 7, verses: 18 },
      { uid: "user3", name: "이소망", streak: 5, verses: 15 }
    ],

    // 미접속 멤버 (3일 이상)
    inactiveMembers: [
      { uid: "user4", name: "최요셉", daysSinceActive: 5 },
      { uid: "user5", name: "한은혜", daysSinceActive: 3 }
    ]
  }
}
```

---

## 3. One-Tap Interaction (원탭 인터랙션)

### 3.1 Quick Reactions (빠른 반응)

**목적**: 최소한의 노력으로 그룹원 격려

#### UI 디자인

```
┌─────────────────────────────────────────────┐
│  🎉 김성실 님이 말라기 1:5 암송 완료!        │
│  2분 전                                      │
│                                              │
│  ┌────────┐ ┌────────┐ ┌────────┐           │
│  │  👏    │ │  🙏    │ │  💪    │           │
│  │  박수  │ │  기도  │ │ 화이팅 │           │
│  │  12    │ │   5    │ │   3    │           │
│  └────────┘ └────────┘ └────────┘           │
│                                              │
└─────────────────────────────────────────────┘
```

#### 3.1.1 반응 타입 상세

| 반응 | 이모지 | 의미 | 알림 메시지 | 햅틱 |
|------|--------|------|------------|------|
| Clap | 👏 | 축하/격려 | `{name}님이 박수를 보냈어요!` | Light |
| Pray | 🙏 | 기도 약속 | `{name}님이 기도해주겠대요!` | Medium |
| Fighting | 💪 | 응원 | `{name}님이 응원해요!` | Light |

#### 3.1.2 반응 UX Flow

```
[탭 1회] → 반응 추가 + 애니메이션 + 햅틱
[탭 2회 (이미 반응함)] → 반응 취소 (토글)
[길게 누르기] → 반응한 사람 목록 보기
```

**애니메이션 상세**:
- 👏 탭 시: 박수 이모지가 위로 튀어오르며 사라짐
- 🙏 탭 시: 이모지가 반짝이며 커졌다 작아짐
- 💪 탭 시: 이모지가 좌우로 흔들림

#### 3.1.3 반응 알림 배치 처리

**문제**: 반응마다 즉시 알림 → 알림 피로 + FCM 비용

**해결**: 5분 단위 배치 처리
```javascript
// Cloud Function: 5분마다 실행
exports.batchReactionNotifications = functions.pubsub
  .schedule('every 5 minutes')
  .onRun(async () => {
    // 최근 5분간 반응 집계
    const recentReactions = await getReactionsSince(5);

    // 사용자별로 그룹핑
    const byUser = groupBy(recentReactions, 'targetUserId');

    // 배치 알림 전송
    for (const [userId, reactions] of Object.entries(byUser)) {
      if (reactions.length === 1) {
        // 단일 반응: 개별 메시지
        sendNotification(userId, `${reactions[0].fromName}님이 박수를 보냈어요!`);
      } else {
        // 다중 반응: 요약 메시지
        sendNotification(userId, `${reactions[0].fromName}님 외 ${reactions.length - 1}명이 응원해요!`);
      }
    }
  });
```

#### 3.1.4 Firestore 업데이트 (비용 최적화)

```dart
// 반응 추가 (배열 + 카운트 동시 업데이트)
Future<void> addReaction(String activityId, String reactionType) async {
  final uid = currentUser.uid;
  final activityRef = _firestore.doc('groups/$groupId/activities/$activityId');

  await activityRef.update({
    'reactions.$reactionType': FieldValue.arrayUnion([uid]),
    'reactionCounts.$reactionType': FieldValue.increment(1),
  });
}

// 반응 제거
Future<void> removeReaction(String activityId, String reactionType) async {
  final uid = currentUser.uid;
  final activityRef = _firestore.doc('groups/$groupId/activities/$activityId');

  await activityRef.update({
    'reactions.$reactionType': FieldValue.arrayRemove([uid]),
    'reactionCounts.$reactionType': FieldValue.increment(-1),
  });
}
```

---

### 3.2 Nudge System (찌르기)

**목적**: 비활성 그룹원 부드럽게 독려

#### 3.2.1 Nudge 트리거 조건

| 조건 | UI 표시 | 찌르기 버튼 |
|------|---------|------------|
| 3일 미접속 | 😴 표시 | 활성화 |
| 7일 미접속 | 😴😴 표시 | 활성화 + 강조 |
| 14일 이상 | 💤 표시 | 활성화 + 빨간 점 |

#### 3.2.2 Nudge UI Flow

**Step 1: 찌르기 버튼 탭**
```
┌─────────────────────────────────────────────┐
│  💌 이소망 님에게 찌르기                     │
│                                              │
│  메시지 선택:                                │
│  ┌─────────────────────────────────────────┐│
│  │ 🙏 "함께 암송해요! 잠깐이라도 들러주세요~" ││
│  └─────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────┐│
│  │ 💕 "보고 싶어요! 오늘 말라기 1절 어때요?" ││
│  └─────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────┐│
│  │ 💪 "우리 그룹이 기다려요! 화이팅!"       ││
│  └─────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────┐│
│  │ ✏️ 직접 작성하기                         ││
│  └─────────────────────────────────────────┘│
│                                              │
│  [취소]                   [보내기]           │
└─────────────────────────────────────────────┘
```

**Step 2: 수신자 화면 (푸시 알림 + 인앱)**
```
┌─────────────────────────────────────────────┐
│  💌 김성실님의 찌르기!                       │
│                                              │
│  "함께 암송해요! 잠깐이라도 들러주세요~ 🙏"   │
│                                              │
│  [무시]          [암송하러 가기]              │
└─────────────────────────────────────────────┘
```

#### 3.2.3 Nudge 제한 규칙

| 규칙 | 제한 |
|------|------|
| 발신자 일일 제한 | 3회/일 |
| 동일 대상 제한 | 24시간 내 1회 |
| 수신자 "조용히" 설정 시 | 알림 미발송 (표시만) |
| 그룹 리더 특권 | 일일 10회까지 |

#### 3.2.4 Nudge Firestore Schema

```javascript
// Collection: users/{uid}/nudges/{nudgeId}
{
  id: "nudge_abc123",
  fromUserId: "user1",
  fromUserName: "김성실",
  toUserId: "user2",
  message: "함께 암송해요! 잠깐이라도 들러주세요~ 🙏",
  templateId: "pray",              // 템플릿 사용 시
  createdAt: Timestamp,
  readAt: null,                    // 읽은 시간
  respondedAt: null,               // 반응 시간 (앱 접속)
  groupId: "group123"
}

// 발신자 일일 카운트 (비용 최적화)
// Collection: users/{uid}/dailyStats/{date}
{
  nudgesSent: 2,
  nudgesTo: {
    "user2": Timestamp,            // 마지막 찌르기 시간
    "user3": Timestamp
  }
}
```

#### 3.2.5 Nudge 효과 측정

```javascript
// Cloud Function: 매일 자정 통계 집계
exports.calculateNudgeEffectiveness = functions.pubsub
  .schedule('0 0 * * *')
  .onRun(async () => {
    // 어제 보낸 nudge 중 24시간 내 앱 접속한 비율
    const nudges = await getNudgesSentYesterday();
    const responded = nudges.filter(n => n.respondedAt != null);

    const effectivenessRate = responded.length / nudges.length;

    await saveMetric('nudge_effectiveness', effectivenessRate);
  });
```

---

## 4. Collaborative Goal (그룹 목표)

### 4.1 Weekly Group Challenge

**목적**: 그룹 단위 목표 달성으로 공동체 의식 강화

#### UI 디자인

```
┌─────────────────────────────────────────────┐
│  🏛️ 이번 주 그룹 챌린지                      │
│  "함께 성전을 쌓아요!"                       │
│                                              │
│       ⛪                                     │
│      /██\        73% 달성!                  │
│     /████\       ███████████░░░              │
│    /██████\                                  │
│   ──────────     목표: 40절 암송             │
│                  현재: 29절                  │
│                  남은 시간: 3일 14시간        │
│                                              │
│  ─────────────────────────────────────────  │
│  🏆 이번 주 기여자                           │
│  1. 김성실 🥇 8절   ████████                 │
│  2. 박믿음 🥈 6절   ██████                   │
│  3. 이소망 🥉 5절   █████                    │
│  4. 최요셉    4절   ████                     │
│  5. 한은혜    3절   ███                      │
│  ... 외 3명                                  │
│                                              │
│  ─────────────────────────────────────────  │
│  내 기여: 8절 (20%)  [암송하러 가기]         │
└─────────────────────────────────────────────┘
```

#### 4.1.1 챌린지 목표 타입

| 목표 타입 | 아이콘 | 테마 | 목표 예시 | 난이도 |
|----------|--------|------|----------|--------|
| 구절 암송 | 🏛️ | 성전 쌓기 | 40절 | 기본 |
| 달란트 모으기 | 💰 | 보물 모으기 | 200 달란트 | 쉬움 |
| 전원 참여 | 👥 | 함께 걷기 | 12명 전원 1절+ | 어려움 |
| 스트릭 유지 | 🔥 | 불꽃 지키기 | 80% 스트릭 유지 | 보통 |
| 책 완독 | 📖 | 말씀 완주 | 말라기 전체 | 도전 |

#### 4.1.2 챌린지 자동 생성 규칙

```javascript
// Cloud Function: 매주 월요일 00:00 (KST)
exports.createWeeklyChallenge = functions.pubsub
  .schedule('0 0 * * 1')
  .timeZone('Asia/Seoul')
  .onRun(async () => {
    const groups = await getAllActiveGroups();

    for (const group of groups) {
      // 그룹 규모에 따른 목표 조정
      const memberCount = group.memberCount;
      const avgWeeklyVerses = group.stats.avgWeeklyVerses || 20;

      // 목표 = 평균 주간 암송 × 1.2 (20% 도전적)
      const targetVerses = Math.ceil(avgWeeklyVerses * 1.2);

      await createChallenge(group.id, {
        type: 'verses',
        target: targetVerses,
        theme: selectRandomTheme(),
        weekStart: getMonday(),
        weekEnd: getSunday(),
        reward: { dalants: 5, badge: 'weekly_winner' }
      });
    }
  });
```

#### 4.1.3 기여 계산 로직

| 활동 | 기여도 |
|------|--------|
| Stage 1 통과 | 1 포인트 |
| Stage 2 통과 | 1 포인트 (중복 불가) |
| Stage 3 통과 | 1 포인트 (중복 불가) |
| 그룹 부스터 사용 중 | 2배 기여 |

**중복 방지**: 같은 구절은 Stage 3까지 통과해도 1 포인트만 인정

#### 4.1.4 챌린지 완료 보상

**개인 보상**:
| 조건 | 보상 |
|------|------|
| 챌린지 참여 (1절+) | 달란트 2개 |
| 상위 50% 기여 | 달란트 3개 추가 |
| 1등 기여자 | 달란트 5개 추가 + "MVP" 배지 |

**그룹 보상** (목표 달성 시):
| 보상 | 내용 |
|------|------|
| 전원 보너스 달란트 | 5개 |
| 그룹 배지 | "이번 주 승리" |
| 연속 달성 | 2주 연속 시 +10 달란트 |

#### 4.1.5 챌린지 완료 Celebration

```
┌─────────────────────────────────────────────┐
│                                              │
│           🎊 축하합니다! 🎊                  │
│                                              │
│        우리 그룹이 이번 주 목표를             │
│           함께 달성했어요!                   │
│                                              │
│              ⛪ 성전 완성! ⛪                 │
│            40절 / 40절 (100%)               │
│                                              │
│  ─────────────────────────────────────────  │
│  🏆 MVP: 김성실 (12절 기여)                  │
│                                              │
│  📦 보상 지급 완료!                          │
│  • 참여 보상: 달란트 +2                      │
│  • 상위 50%: 달란트 +3                       │
│  • 그룹 달성: 달란트 +5                      │
│  ─────────────────────────────────────────  │
│  총 획득: 💰 10 달란트                       │
│                                              │
│  [다음 주 챌린지 보기]     [공유하기]         │
│                                              │
└─────────────────────────────────────────────┘
```

#### 4.1.6 Firestore Schema

```javascript
// Collection: groups/{groupId}/challenges/{weekId}
{
  id: "2026-W04",                   // ISO 주차
  weekStart: Timestamp,            // 2026-01-20 00:00:00
  weekEnd: Timestamp,              // 2026-01-26 23:59:59

  type: "verses",                  // verses | dalants | participation | streak
  theme: "temple",                 // UI 테마
  targetValue: 40,
  currentValue: 29,

  contributors: {
    "user1": { name: "김성실", count: 8, lastContributed: Timestamp },
    "user2": { name: "박믿음", count: 6, lastContributed: Timestamp },
    "user3": { name: "이소망", count: 5, lastContributed: Timestamp }
  },
  contributorCount: 8,

  isCompleted: false,
  completedAt: null,

  reward: {
    dalants: 5,
    badge: "weekly_winner"
  },

  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

---

## 5. Notification Strategy (알림 전략)

### 5.1 알림 유형 및 우선순위

| 유형 | 시간 | 내용 | 우선순위 | 채널 |
|------|------|------|----------|------|
| Morning Manna | 설정 시간 | 오늘의 구절 | HIGH | Push |
| Streak Warning | 21:00 | 스트릭 끊길 위험 | HIGH | Push |
| Nudge Received | 실시간 | 찌르기 받음 | MEDIUM | Push + InApp |
| Goal 50% | 진행률 도달 시 | 목표 절반 달성 | MEDIUM | Push |
| Goal 90% | 진행률 도달 시 | 목표 거의 완료 | MEDIUM | Push |
| Group Activity | 5분 배치 | 그룹원 활동 요약 | LOW | InApp Only |
| Weekly Summary | 일요일 18:00 | 주간 리포트 | LOW | Push |
| Reaction Batch | 5분 배치 | 반응 요약 | LOW | Push |

### 5.2 알림 설정 화면

```
┌─────────────────────────────────────────────┐
│  🔔 알림 설정                                │
│                                              │
│  ─────────────────────────────────────────  │
│  아침 만나                                   │
│  [●] 활성화                     06:00 ▼     │
│                                              │
│  ─────────────────────────────────────────  │
│  스트릭 리마인더                             │
│  [●] 활성화                     21:00 ▼     │
│                                              │
│  ─────────────────────────────────────────  │
│  그룹 활동                                   │
│  [●] 마일스톤만 (7일+, 책 완독 등)           │
│  [ ] 모든 활동                               │
│  [ ] 알림 끄기                               │
│                                              │
│  ─────────────────────────────────────────  │
│  찌르기                                      │
│  [●] 받기                                    │
│  [ ] 조용히 (표시만, 알림 없음)              │
│                                              │
│  ─────────────────────────────────────────  │
│  주간 리포트                                 │
│  [●] 받기                                    │
│                                              │
└─────────────────────────────────────────────┘
```

### 5.3 Smart Batching 로직

```javascript
// Cloud Function: 5분마다 실행
exports.batchNotifications = functions.pubsub
  .schedule('every 5 minutes')
  .onRun(async () => {
    const fiveMinAgo = new Date(Date.now() - 5 * 60 * 1000);

    // 1. 그룹 활동 배치
    const activities = await getActivitiesSince(fiveMinAgo);
    const byGroup = groupBy(activities, 'groupId');

    for (const [groupId, groupActivities] of Object.entries(byGroup)) {
      const members = await getGroupMembers(groupId);

      for (const member of members) {
        // 자신의 활동 제외
        const othersActivities = groupActivities.filter(a => a.userId !== member.uid);

        if (othersActivities.length === 0) continue;
        if (!member.settings.groupActivityNotification) continue;

        // 요약 메시지 생성
        const message = othersActivities.length === 1
          ? `${othersActivities[0].userName}님이 ${othersActivities[0].verseRef} 암송 완료!`
          : `${othersActivities[0].userName}님 외 ${othersActivities.length - 1}명이 활동했어요!`;

        await sendPushNotification(member.fcmToken, {
          title: '그룹 소식',
          body: message,
          data: { type: 'group_activity', groupId }
        });
      }
    }

    // 2. 반응 배치 (별도 처리)
    // ...
  });
```

---

## 6. Firestore Architecture (비용 최적화)

### 6.1 Collection Structure

```
firestore/
├── users/{uid}
│   ├── [Document Fields]
│   │   ├── profile: { name, email, photoUrl, groupId }
│   │   ├── streak: { currentStreak, longestStreak, lastLearnedDate, ... }
│   │   ├── dalants: { balance, totalEarned, totalSpent }
│   │   └── settings: { notifications, privacy, ... }
│   │
│   ├── nudges/ (subcollection)
│   │   └── {nudgeId}: { from, message, createdAt, readAt }
│   │
│   └── dailyStats/ (subcollection)
│       └── {date}: { nudgesSent, versesCompleted, ... }
│
├── groups/{groupId}
│   ├── [Document Fields]
│   │   ├── info: { name, code, description, createdBy }
│   │   ├── stats: { memberCount, todayActive, weeklyVerses, mvp[], inactive[] }
│   │   └── settings: { weeklyGoalType, notificationLevel }
│   │
│   ├── members/ (subcollection)
│   │   └── {uid}: { joinedAt, role, lastActiveAt }
│   │
│   ├── activities/ (subcollection, TTL 7일)
│   │   └── {activityId}: { type, userId, userName, reactions, createdAt, expiresAt }
│   │
│   └── challenges/ (subcollection)
│       └── {weekId}: { type, target, current, contributors, isCompleted }
│
└── global/
    ├── dailyVerse: { reference, text, textKo, date, season }
    └── seasonalVerses: { newYear[], easter[], christmas[], ... }
```

### 6.2 Read Cost 최적화

| 기능 | 쿼리 방식 | 예상 reads/user/day | 최적화 전략 |
|------|----------|---------------------|-------------|
| Activity Feed | lastFetchTime 이후만 | ~5 | Local cache + pull-to-refresh |
| Group Stats | 캐시된 doc 읽기 | ~3 | 5분 TTL 캐시 |
| Leaderboard | 주 1회 계산된 필드 | ~1 | Cloud Function 사전 계산 |
| User Profile | 세션 시작 시 1회 | ~1 | SharedPreferences 캐시 |
| Challenge Status | 그룹 접근 시 | ~2 | 그룹 doc에 임베드 |
| **Total** | | **~12** | |

### 6.3 Write Cost 최적화

| 이벤트 | 쓰기 방식 | 예상 writes/user/day | 최적화 전략 |
|--------|----------|----------------------|-------------|
| Verse Complete | 배치 (5분) | ~2 | Activity 생성 병합 |
| Streak Update | 1일 1회 | 1 | 날짜 변경 시만 |
| Activity Create | 배치 (5분) | ~2 | Cloud Function 배치 |
| Reaction | 즉시 (배열) | ~3 | arrayUnion 사용 |
| Challenge Update | 암송 시 | ~2 | increment 사용 |
| **Total** | | **~10** | |

### 6.4 예상 월간 비용 (1,000 DAU 기준)

| 항목 | 계산 | 월 예상 비용 |
|------|------|-------------|
| Reads | 1,000 × 12 × 30 = 360,000 | ~$0.22 (50K free) |
| Writes | 1,000 × 10 × 30 = 300,000 | ~$0.30 (20K free) |
| Storage | ~500MB | ~$0.09 |
| Functions | ~100K invocations | Free tier |
| **Total** | | **~$0.61/month** |

---

## 7. Implementation Phases

### Phase 1: Foundation (1주차)

| Task | 우선순위 | 예상 공수 | 담당 |
|------|----------|----------|------|
| Streak 서비스 구현 | HIGH | 2일 | Backend |
| Streak UI (홈 화면) | HIGH | 1일 | Frontend |
| 달란트 서비스 구현 | HIGH | 1일 | Backend |
| 마일스톤 보상 로직 | MEDIUM | 1일 | Backend |
| 스트릭 보호권 UI | MEDIUM | 0.5일 | Frontend |

**완료 기준**:
- [ ] 스트릭 카운트 정상 동작
- [ ] 마일스톤 도달 시 달란트 지급
- [ ] 스트릭 보호권 구매 및 사용

### Phase 2: Social Core (2주차)

| Task | 우선순위 | 예상 공수 | 담당 |
|------|----------|----------|------|
| Activity Feed 서비스 | HIGH | 2일 | Backend |
| Activity Ticker UI | HIGH | 1.5일 | Frontend |
| Quick Reactions | HIGH | 1일 | Full-stack |
| Nudge 서비스 | MEDIUM | 1일 | Backend |
| Nudge UI | MEDIUM | 1일 | Frontend |
| 그룹 대시보드 | MEDIUM | 1.5일 | Frontend |

**완료 기준**:
- [ ] 활동 피드 실시간 갱신
- [ ] 반응 추가/취소 동작
- [ ] 찌르기 발송 및 수신

### Phase 3: Gamification (3주차)

| Task | 우선순위 | 예상 공수 | 담당 |
|------|----------|----------|------|
| Weekly Challenge 서비스 | HIGH | 2일 | Backend |
| Challenge 자동 생성 Function | HIGH | 1일 | Backend |
| Challenge Progress UI | HIGH | 1.5일 | Frontend |
| Celebration 화면 | MEDIUM | 0.5일 | Frontend |
| Morning Manna 서비스 | MEDIUM | 1일 | Backend |
| Early Bird 보너스 | MEDIUM | 0.5일 | Backend |

**완료 기준**:
- [ ] 주간 챌린지 자동 생성
- [ ] 기여도 실시간 반영
- [ ] 목표 달성 시 보상 지급

### Phase 4: Polish (4주차)

| Task | 우선순위 | 예상 공수 | 담당 |
|------|----------|----------|------|
| Push Notification 구현 | HIGH | 2일 | Full-stack |
| 알림 배치 처리 Function | HIGH | 1일 | Backend |
| 알림 설정 UI | MEDIUM | 1일 | Frontend |
| 성능 최적화 | HIGH | 1일 | Full-stack |
| 비용 모니터링 설정 | MEDIUM | 0.5일 | Backend |
| QA 및 버그 수정 | HIGH | 2일 | Full-stack |

**완료 기준**:
- [ ] 모든 알림 정상 발송
- [ ] 배치 처리로 비용 최적화
- [ ] 목표 비용 범위 내 운영

---

## 8. Success Metrics (성공 지표)

### 8.1 핵심 KPI

| 지표 | 목표 | 측정 방법 | 측정 주기 |
|------|------|----------|----------|
| DAU/MAU | 40%+ | Firebase Analytics | 주간 |
| 7일 리텐션 | 50%+ | Cohort Analysis | 주간 |
| 평균 세션 시간 | 5분+ | Analytics | 주간 |
| 그룹 가입률 | 70%+ | 가입 유저 중 그룹 가입 비율 | 주간 |
| 스트릭 7일+ 비율 | 30%+ | 활성 유저 중 비율 | 주간 |

### 8.2 Engagement 지표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 일일 암송 완료 | 3절+ | 유저별 평균 |
| Nudge 응답률 | 30%+ | 찌르기 후 24시간 내 접속 |
| 반응 참여율 | 50%+ | 활동 대비 반응 비율 |
| 챌린지 달성률 | 60%+ | 주간 목표 달성 그룹 비율 |
| Morning Manna 참여 | 25%+ | 아침 8시 전 암송 비율 |

### 8.3 비용 지표

| 지표 | 목표 | 알림 기준 |
|------|------|----------|
| Firestore Reads/user/day | <15 | >20 시 알림 |
| Firestore Writes/user/day | <12 | >15 시 알림 |
| FCM 월 발송 | <500K | >400K 시 알림 |
| 월간 총 비용 | <$50 | >$40 시 알림 |

---

## 9. Risk & Mitigation (위험 및 대응)

| 리스크 | 영향도 | 발생 가능성 | 대응 방안 |
|--------|--------|------------|----------|
| Firestore 비용 폭발 | HIGH | MEDIUM | TTL, 배치 처리, 실시간 모니터링 |
| 알림 피로 | MEDIUM | HIGH | 스마트 배칭, 사용자 설정, 채널 분리 |
| 그룹 비활성화 | MEDIUM | MEDIUM | AI 자동 Nudge, 관리자 알림, 챌린지 |
| 경쟁 과열 | LOW | LOW | 협력 강조, 순위 대신 기여도 표시 |
| 스트릭 압박감 | MEDIUM | MEDIUM | 보호권, 긍정적 메시지, 복구 유도 |

---

## 10. Appendix

### A. 메시지 템플릿 전체 목록

**Activity 메시지**:
```
verse_complete: "{name}님이 {verse} 암송 완료!"
stage3_clear: "{name}님이 {verse} 완전 암기!"
streak_milestone: "{name}님이 {n}일 연속 학습 달성!"
book_complete: "{name}님이 {book} 완독!"
joined_group: "{name}님이 그룹에 합류했어요!"
book_start: "{name}님이 {book} 시작!"
comeback: "{name}님이 돌아왔어요!"
```

**Nudge 템플릿**:
```
pray: "함께 암송해요! 잠깐이라도 들러주세요~ 🙏"
miss: "보고 싶어요! 오늘 말라기 1절 어때요? 💕"
fighting: "우리 그룹이 기다려요! 화이팅! 💪"
custom: (사용자 직접 입력)
```

**Push 알림**:
```
morning_manna: "☀️ 오늘의 만나가 도착했어요! {verse}"
streak_warning: "🔥 스트릭이 끊어질 위험! 오늘 암송하고 {n}일 유지하세요"
nudge_received: "💌 {name}님의 찌르기: {message}"
challenge_50: "🏛️ 그룹 목표 50% 달성! 조금만 더 힘내요!"
challenge_complete: "🎉 축하해요! 이번 주 그룹 목표 달성!"
reaction_batch: "👏 {name}님 외 {n}명이 응원해요!"
weekly_summary: "📊 이번 주 {verses}절 암송! 다음 주도 화이팅!"
```

### B. Key Decisions 요약

| 결정 사항 | 이유 |
|----------|------|
| 실시간 리스너 대신 폴링 | Firestore 연결 비용 절감 |
| 활동 7일 TTL | 저장 비용 절감 + 쿼리 성능 |
| 반응 배열 + 카운트 동시 저장 | 읽기 최적화 (카운트만 표시 가능) |
| 5분 배치 알림 | FCM 비용 + 알림 피로 방지 |
| 그룹 stats 임베드 | 별도 쿼리 없이 대시보드 표시 |
| MVP 사전 계산 | 실시간 정렬 쿼리 방지 |

---

*"혼자 가면 빨리 가고, 함께 가면 멀리 간다."*

**문서 끝**
